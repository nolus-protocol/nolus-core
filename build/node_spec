FROM alpine:3.14
ARG BIN_NAME=nolusd
ARG TARGET=release

COPY target/${TARGET}/${BIN_NAME} /usr/bin/

# tendermint p2p
EXPOSE 26656/tcp
# tendermint rpc
EXPOSE 26657/tcp

ENTRYPOINT ["nolusd"]
CMD ["version"]